[mypy]
# Single-file checks (editor, pre-commit hooks) must resolve `scripts.*` the
# same way the unit tests do: from the repository root.
mypy_path = .
explicit_package_bases = True
namespace_packages = True
